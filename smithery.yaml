# Smithery configuration for Deployment Test Server
runtime: "container"

startCommand:
  type: "http"
  configSchema:
    type: "object"
    properties: {}
    required: []
  exampleConfig: {}

build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."

env:
  PYTHONUNBUFFERED: "1"
